let radiusCircle = '5 см',
    radiusCircleNum = parseInt(radiusCircle),
    SkrugaResult = Math.PI * Math.pow(radiusCircleNum, 2)

let Skruga = SkrugaResult.toFixed(2) + 'см^2';

console.log(Skruga);

